<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ljp.xjt.mapper.ClassesMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.ljp.xjt.entity.Classes">
        <id column="id" property="id" />
        <result column="class_name" property="className" />
        <result column="class_code" property="classCode" />
        <result column="grade_year" property="gradeYear" />
        <result column="major_id" property="majorId" />
        <result column="advisor_teacher_id" property="advisorTeacherId" />
        <result column="created_time" property="createdTime" />
        <result column="updated_time" property="updatedTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, class_name, class_code, grade_year, major_id, advisor_teacher_id, created_time, updated_time
    </sql>

    <!-- 如果有自定义SQL，可以在这里添加 -->
    <!-- 
    <select id="findClassesByAdvisorTeacherId" resultMap="BaseResultMap">
        SELECT 
        <include refid="Base_Column_List" />
        FROM classes
        WHERE advisor_teacher_id = #{advisorTeacherId}
    </select>
    -->

</mapper> 