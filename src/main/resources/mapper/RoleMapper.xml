<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ljp.xjt.mapper.RoleMapper">

    <!-- 根据用户ID查询该用户拥有的所有角色 -->
    <select id="findRolesByUserId" resultType="com.ljp.xjt.entity.Role">
        SELECT 
            r.id,
            r.role_name AS roleName,
            r.role_code AS roleCode,
            r.description,
            r.created_time
        FROM 
            roles r
        INNER JOIN 
            user_roles ur ON r.id = ur.role_id
        WHERE 
            ur.user_id = #{userId}
    </select>

</mapper> 